import { ChangeEvent, DragEvent, useState } from "react";
import { CheckCircle2, FileUp, LoaderCircle, UploadCloud } from "lucide-react";
import { api } from "../services/api";
const MAX = 20 * 1024 * 1024; const types = ["application/pdf", "text/plain", "application/vnd.openxmlformats-officedocument.wordprocessingml.document"];
export function DocumentsPage() { const [file, setFile] = useState<File | null>(null), [busy, setBusy] = useState(false), [error, setError] = useState(""), [success, setSuccess] = useState(""); const choose = (candidate?: File) => { setError(""); setSuccess(""); if (!candidate) return; if (!types.includes(candidate.type) && !/\.(pdf|docx|txt)$/i.test(candidate.name)) return setError("Only PDF, DOCX and TXT files are supported."); if (candidate.size > MAX) return setError("Maximum upload size is 20 MB."); setFile(candidate); }; const upload = async () => { if (!file) return setError("Choose a document first."); setBusy(true); setError(""); try { const encoded = await readFile(file); const response = await api.upload(file.name, encoded); setSuccess(`${response.message} ${response.chunk_count} chunks indexed.`); setFile(null); } catch (e) { setError(e instanceof Error ? e.message : "Unable to upload document."); } finally { setBusy(false); } }; return <div className="content-page"><div className="page-intro"><span className="eyebrow">HR DOCUMENT MANAGEMENT</span><h1>Documents</h1><p>Upload enterprise content to index it in the knowledge base.</p></div><label className={`dropzone ${file ? "has-file" : ""}`} onDragOver={e => e.preventDefault()} onDrop={(e: DragEvent) => { e.preventDefault(); choose(e.dataTransfer.files[0]); }}><input type="file" accept=".pdf,.docx,.txt" onChange={(e: ChangeEvent<HTMLInputElement>) => choose(e.target.files?.[0])}/>{file ? <><FileUp size={30}/><b>{file.name}</b><span>{(file.size / 1024 / 1024).toFixed(2)} MB · ready to index</span></> : <><UploadCloud size={32}/><b>Drag and drop your document here</b><span>PDF, DOCX or TXT · Maximum 20 MB</span><em>Browse files</em></>}</label><button className="primary-button upload-button" onClick={upload} disabled={!file || busy}>{busy ? <><LoaderCircle className="spin"/> Uploading and indexing…</> : <><FileUp size={18}/> Upload and index document</>}</button>{error && <div className="form-error">{error}</div>}{success && <div className="upload-success"><CheckCircle2/>{success}</div>}</div> }
function readFile(file: File) { return new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result).split(",")[1] || ""); reader.onerror = reject; reader.readAsDataURL(file); }); }
